package com.wildmind.fanwave.animation;

import android.view.animation.AlphaAnimation;

public class AlphaSetter extends AlphaAnimation {

	public AlphaSetter (float alpha) {
		super(alpha, alpha);
		setDuration(0);
		setFillAfter(true);
	}
}
